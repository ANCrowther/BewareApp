namespace Beware.Utilities {
    public enum Direction {
        Down,
        Left,
        Right,
        Up
    }

    public enum View {
        GamePlay,
        InfoBottom,
        InfoLeft,
        InfoRight,
        Menu,
        Ticker,
    }

    public enum Mode {
        Move,
        Shoot
    }

    public enum Resolution {
        Game,
        Menu
    }

    public enum ViewportLayout {
        Layout1,
        Layout2
    }
}
